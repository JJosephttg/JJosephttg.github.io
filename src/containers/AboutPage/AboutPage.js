import React from 'react';

import styles from './AboutPage.module.css';

const AboutPage = _ => {
    console.log(styles);
    return (
        <h1 className={styles.Title}>Josh Joseph</h1>
    );
}

export default AboutPage;